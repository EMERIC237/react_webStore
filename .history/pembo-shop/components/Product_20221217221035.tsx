import React from 'react'
import Link from 'next/link'

import { urlFor } from '../lib/client'

const Product = ({product: {image, name, slug, }}:any) => {
  return (
    <div>Product</div>
  )
}

export default Product